name: Deploy → Test Server

on:
  push:
    branches:
      - develop

concurrency:
  group: deploy-test
  cancel-in-progress: false  # queue, don't cancel in-flight deployments

jobs:
  build:
    name: Build & Push (develop)
    uses: ./.github/workflows/_docker-build-push.yml
    with:
      image_tag: develop
    secrets:
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}

  deploy:
    name: SSH Deploy to Test
    needs: build
    runs-on: ubuntu-latest
    environment: test

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HETZNER_TEST_HOST }}
          username: ${{ secrets.HETZNER_SSH_USER }}
          key: ${{ secrets.HETZNER_TEST_SSH_KEY }}
          script: |
            set -euo pipefail

            cd /opt/dreamanything/test

            echo "==> Updating compose file..."
            curl -fsSL "https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}/infra/docker-compose.test.yml" \
              -o docker-compose.yml

            echo "==> Pulling latest images..."
            docker compose pull

            echo "==> Seeding RIS MCP config..."
            python3 -c "
import json, os
config_path = '/data/dreamanything/test/storage/plugins/anythingllm_mcp_servers.json'
os.makedirs(os.path.dirname(config_path), exist_ok=True)
try:
    with open(config_path) as f:
        cfg = json.load(f)
except (FileNotFoundError, json.JSONDecodeError, ValueError):
    cfg = {'mcpServers': {}}
if 'ris' in cfg.get('mcpServers', {}):
    print('    RIS MCP already configured — skipping.')
else:
    cfg.setdefault('mcpServers', {})['ris'] = {
        'url': 'http://ris-mcp:3000/mcp',
        'type': 'streamable',
        'anythingllm': {'autoStart': True}
    }
    with open(config_path, 'w') as f:
        json.dump(cfg, f, indent=2)
    print('    Added RIS MCP server to config.')
"
            chown -R 1000:1000 /data/dreamanything/test/storage/plugins

            echo "==> Starting updated container..."
            docker compose up -d --no-build --remove-orphans

            echo "==> Waiting for health check (up to 90s)..."
            timeout 90 bash -c '
              until docker compose exec -T app curl -sf http://localhost:3001/api/ping > /dev/null 2>&1; do
                echo "  Waiting..."
                sleep 5
              done
            '

            echo "==> Pruning old images..."
            docker image prune -f --filter "until=72h"

            echo "==> Deploy complete."
